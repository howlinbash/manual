# Give User sudo privilages
su
sudo adduser <username> sudo

# If non-free drivers are needed
sudo vim /etc/apt/sources.list
# append to 6 mirror addresses
contrib non-free
# then
sudo apt-get update && sudo apt-get dist-upgrade

# Dark theme, nice terminal
Tweak Tool > Appearance > Global Dark Theme
Terminal > preferences > general >
  show menubar by default in new terminals

load my bash profile
make vim commands
install 
vim vlc tmux flux
